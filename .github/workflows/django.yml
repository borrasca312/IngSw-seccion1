# SGICS - Workflow específico para Django Backend
# Ejecuta tests, linting y análisis de código del backend Django

name: Django Backend CI

# Triggers específicos para backend y ramas del proyecto
on:
  push:
    branches: 
      - main              # Producción
      - develop          # Desarrollo  
      - 'SCRUM-*'        # Features del proyecto SCRUM
      - 'feature/*'      # Features adicionales
      - 'hotfix/*'       # Hotfixes críticos
    paths:
      - 'backend/**'
      - '.github/workflows/django.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'backend/**'

env:
  PYTHON_VERSION: "3.12"

jobs:
  # Tests con múltiples versiones de Python
  django-tests:
    name: "Django Tests (Python ${{ matrix.python-version }}) - ${{ github.ref_name }}"
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]  # Soporte múltiples versiones
      fail-fast: false  # Continúa aunque falle una versión
    
    services:
      # Base de datos de prueba
      mysql:
        image: mysql:8.0
        env:
          MYSQL_DATABASE: test_sgics_${{ github.run_id }}  # DB única por run
          MYSQL_USER: sgics_test
          MYSQL_PASSWORD: test_pass_secure_2024
          MYSQL_ROOT_PASSWORD: root_pass_secure_2024
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3
      
      # Cache Redis para tests
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 3
    
    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4
    
    - name: 🐍 Setup Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'
        cache-dependency-path: 'backend/requirements.txt'
    
    - name: 📦 Instalar dependencias Django
      working-directory: ./backend
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install coverage pytest-django pytest-cov
    
    - name: Install setuptools
      run: pip install --upgrade setuptools
    
    - name: 🔍 Verificar configuración Django
      working-directory: ./backend
      env:
        DATABASE_URL: mysql://sgics_test:test_pass_secure_2024@localhost:3306/test_sgics_${{ github.run_id }}
        DJANGO_SETTINGS_MODULE: scouts_platform.settings.development
        SECRET_KEY: test-secret-key-for-ci
      run: |
        python manage.py check --deploy
        python manage.py check
    
    - name: 🗃️ Ejecutar migraciones Django
      working-directory: ./backend
      env:
        DATABASE_URL: mysql://sgics_test:test_pass_secure_2024@localhost:3306/test_sgics_${{ github.run_id }}
        DJANGO_SETTINGS_MODULE: scouts_platform.settings.development
        SECRET_KEY: test-secret-key-for-ci
      run: |
        python manage.py migrate --noinput
    
    - name: 🧪 Ejecutar test suite Django
      working-directory: ./backend
      env:
        DATABASE_URL: mysql://sgics_test:test_pass_secure_2024@localhost:3306/test_sgics_${{ github.run_id }}
        DJANGO_SETTINGS_MODULE: scouts_platform.settings.development
        SECRET_KEY: test-secret-key-for-ci
      run: |
        python manage.py test --keepdb --parallel --verbosity=2
          echo "🔍 Verificando /healthz/readyz"
          curl -f http://localhost:8000/healthz/readyz/ || exit 1
    
    - name: 📊 Coverage con pytest-django
      working-directory: ./backend
      env:
        DATABASE_URL: mysql://sgics_test:test_pass_secure_2024@localhost:3306/test_sgics_${{ github.run_id }}
        DJANGO_SETTINGS_MODULE: scouts_platform.settings.development
        SECRET_KEY: test-secret-key-for-ci
      run: |
        coverage run --source='.' manage.py test --keepdb
        coverage xml
        coverage report --show-missing

    - name: 📤 Upload coverage
      uses: actions/upload-artifact@v4
      with:
        name: django-coverage-${{ matrix.python-version }}-${{ github.ref_name }}
        path: backend/coverage.xml

  # Análisis de calidad de código Django
  django-quality:
    name: "Django Code Quality - ${{ github.ref_name }}"
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4
    
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
    - name: 📦 Instalar herramientas de calidad
      working-directory: ./backend
      run: |
        pip install flake8 black isort bandit safety pylint django-upgrade
    
    - name: 🎨 Reformat code with Black
      working-directory: ./backend
      run: black .
    
    - name: 🛡️ Verificar vulnerabilidades en dependencias
      working-directory: ./backend
      run: safety check --json
    
    - name: 📊 Análisis con Pylint (Django específico)
      working-directory: ./backend
      run: |
        pylint --load-plugins=pylint_django --django-settings-module=scouts_platform.settings.development apps/
      continue-on-error: true  # No fallar el build por warnings de pylint


