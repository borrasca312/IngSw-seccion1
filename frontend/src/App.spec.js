import { describe, it, expect } from 'vitest'

// Prueba mínima para validar pipeline y cobertura

describe('smoke', () => {
    it('debería ser verdadero', () => {
        expect(true).toBe(true)
    })
})
