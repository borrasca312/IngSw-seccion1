[pytest]
DJANGO_SETTINGS_MODULE = scout_project.settings
python_files = test*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --tb=short
    --strict-markers
    --disable-warnings
testpaths = .
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
